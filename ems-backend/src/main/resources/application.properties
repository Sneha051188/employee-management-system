spring.application.name=ems-backend

# MySQL Database Configuration (uses environment variables for production)
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/companydb}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Prateek984123@#*}

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=${PORT:8081}

# CORS Configuration (allow all origins for deployed frontend)
spring.web.cors.allowed-origins=*
